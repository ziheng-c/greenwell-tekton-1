apiVersion: tekton.dev/v1alpha1
kind: Pipeline
metadata:
  name: sample-pipeline
spec:
  tasks:
    - name: task1
      taskRef:
        name: task1
    - name: echo-hello-world
      taskRef:
        name: echo-hello-world    